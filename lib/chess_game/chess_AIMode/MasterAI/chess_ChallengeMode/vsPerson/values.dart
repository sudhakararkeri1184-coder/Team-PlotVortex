import 'package:flutter/material.dart';

class ChessColors {
  static const Color background = Colors.grey;
  static const Color whiteSquare = Color(
    0xFFEEDDB5,
  ); // Classic chess board color
  static const Color blackSquare = Color(
    0xFF9C7C4C,
  ); // Classic chess board color
}
